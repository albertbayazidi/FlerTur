# TASKS
- Add cron job
    - Could be nice to have cronjob changes automatily retrived from repo.
- Add concurrency  
    - Try adding concurrency in the form of having two threads, each with 7 tabs and one browser
    - Try converting each function call to collect data into a separate process so each can be handled concurrently
- Add sorting by price, day
- Remove " from Tog database, not sure if its an issue at go level, og db level


# FINISHED TASKS
- Only get the url of the cheapest route
- Add data collection function that reads a page after the full list has been retrieved
- Add frontend interaction with a table of prices and dates, (could be sent to the user with the page)

